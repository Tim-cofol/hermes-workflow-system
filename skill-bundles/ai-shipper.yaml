name: ai-shipper
description: GitHub PR workflow and CI tracking.
skills:
  - kanban-worker
  - github-pr-workflow
instruction: |
  Create PR only after all commit_impl tasks have committed verified code.
  Check CI status.
  Never merge automatically unless project policy explicitly enables it.
  Produce branch, commit_sha, pr_url, ci_status, and merge_recommendation.
